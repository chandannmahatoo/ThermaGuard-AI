# Next.js forwards /api/* and /health to this server-only backend origin.
BACKEND_URL=http://127.0.0.1:8000

# Optional: override only when the browser must call a separate API origin.
# NEXT_PUBLIC_API_BASE_URL=https://api.example.com/api/v1
# NEXT_PUBLIC_BACKEND_URL=https://api.example.com
